#include <stdio.h>

int main() {
	
	int a;
  int b;
  int aux;

  scanf("%d%d", &a,&b);

  a = aux;
  a = b;
  b = aux;
  
  printf("valor da primeira variavel:%d\nvalor da segunda variavel: %d", a, b);
	
	return 0;
}